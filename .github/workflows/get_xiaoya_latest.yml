name: get latest xiaoya alist
on:
  workflow_dispatch:
jobs:
  container-test-job:
    runs-on: ubuntu-latest
    container:
      image: xiaoyaliu/alist:latest
      volumes:
        - /opt/alist:/mydata
    steps:
      - name: test
        run: cd /mydata && ls -al